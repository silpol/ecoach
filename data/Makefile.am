dbusdir=$(datadir)/dbus-1/services
dbus_DATA=ecoach.service

desktopdir=$(datadir)/applications/hildon
desktop_DATA=ecoach.desktop

datafilesdir = $(datadir)/ecoach
datafiles_DATA =		\
	ec_gpx_ext_v1.xsd	\
	ec_style.rc

EXTRA_DIST = $(dbus_DATA) $(desktop_DATA) $(datafiles_DATA)
